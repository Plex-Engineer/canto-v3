

export async function txVote(
  account: string,
  proposalID: number,
  option: number,
  nodeAddressIP: string,
  fee: Number,
  chain: string,
  memo: string
) {
 
}
export async function getAccountVote(
  proposalID: string,
  nodeAddressIP: string
) {
    
}
